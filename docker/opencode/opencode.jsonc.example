{
  "$schema": "https://opencode.ai/config.json",
  "disabled_providers": ["opencode"],
  "permission": {
    "edit": "allow",
    "bash": "allow",
    "webfetch": "allow",
    "external_directory": "allow"
  }
  // Provider and model are generated per task from the administrator-managed database catalog.
}
