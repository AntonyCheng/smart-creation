# 独立文本审校

当用户只要求审校文本，或公文正文完成后准备生成 Word 时，运行 `scripts/review_text.py`。

检查标题与文种、主送关系、结语、层级跳号、重复句、中文标点、口语和网络表达、空泛套话、绝对化表述、无来源政策表述及字数限制。

处理规则：

- 无来源政策依据、报告夹带请示、正文超过明确上限为阻断问题。
- 权限、事实、数字、绝对化表述和内容缺失为重要问题，要求确认。
- 标点、重复、一般套话和规范结语为优化问题，可在不改变含义时自动修正。
- 独立审校只返回问题清单和建议；用户要求生成文件时，完成审校后仍须生成并校验一个 Word。

